<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Task Manager</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-gray-900 text-white">
    <div class="container mx-auto p-6">
      <!-- Create Task Form -->
      <h1 class="text-2xl font-bold mb-4">Create Task</h1>
      <form action="/task/create" method="POST" class="mb-8">
        <div class="mb-4">
          <input
            type="text"
            name="title"
            id="title"
            placeholder="Title goes here..."
            class="w-full px-4 py-2 rounded bg-gray-800 text-white placeholder-gray-400 focus:outline-none focus:ring focus:ring-blue-500"
            required
          />
        </div>
        <div class="mb-4">
          <textarea
            name="description"
            id="description"
            placeholder="Write your task details here..."
            class="w-full px-4 py-2 rounded bg-gray-800 text-white placeholder-gray-400 focus:outline-none focus:ring focus:ring-blue-500"
            rows="3"
            required
          ></textarea>
        </div>
        <button
          type="submit"
          class="px-6 py-2 bg-blue-500 text-white rounded hover:bg-blue-600 focus:outline-none focus:ring focus:ring-blue-500"
        >
          Create Task
        </button>
      </form>

      <!-- Task List -->
      <h1 class="text-2xl font-bold mb-4">All Tasks</h1>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <% if (tasks && tasks.length > 0) { %> <% tasks.forEach(function(task) {
        %>
        <div class="bg-gray-800 p-4 rounded shadow-md">
          <h2 class="text-lg font-semibold mb-2"><%= task.title %></h2>
          <p class="text-gray-300 mb-4"><%= task.description %></p>
          <div class="flex justify-between">
            <a
              href="/task/update/<%= task._id %>"
              class="px-4 py-2 bg-yellow-500 text-white rounded hover:bg-yellow-600 focus:outline-none focus:ring focus:ring-yellow-500"
            >
              Update
            </a>
            <form
              action="/task/delete/<%= task._id %>"
              method="POST"
              class="inline"
            >
              <button
                type="submit"
                class="px-4 py-2 bg-red-500 text-white rounded hover:bg-red-600 focus:outline-none focus:ring focus:ring-red-500"
              >
                Delete
              </button>
            </form>
          </div>
        </div>
        <% }) %> <% } else { %>
        <p class="text-gray-400">
          No tasks available. Create a new task above.
        </p>
        <% } %>
      </div>
    </div>
  </body>
</html>
